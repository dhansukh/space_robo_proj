and now drone doesnt fly up it just arms and hover at 0.03 meters 

● Here's what changed and why:         
                                                                     
  New state machine:      
  INIT → FLY_ENTRY → FLY_MID → FIND_PASS → LIDAR_NAV → RETURN → LANDING →
  DONE                                                                      
                                                                            
  ┌───────────┬──────────────────────────────────────────────────────────┐  
  │   State   │                       What happens                       │  
  ├───────────┼──────────────────────────────────────────────────────────┤
  │ FLY_ENTRY │ Splines to (0, 0, -2.2) NED offset from home — the cave  │
  │           │ entry sphere                                             │
  ├───────────┼──────────────────────────────────────────────────────────┤  
  │ FLY_MID   │ Splines to (0, 0, -5.0) NED offset — the mid-chamber     │
  │           │ scan point                                               │  
  ├───────────┼──────────────────────────────────────────────────────────┤
  │           │ Hovers here; checks max clearance across all 36 sectors. │  
  │ FIND_PASS │  When any sector ≥ 5 m is found, that's the passage —    │  
  │           │ flies into it                                            │
  ├───────────┼──────────────────────────────────────────────────────────┤  
  │           │ Continuously refreshes waypoint from best scored sector  │
  │ FLY_ENTRY │ Splines to (0, 0, -2.2) NED offset from home — the cave  │
  │           │ entry sphere                                             │
  ├───────────┼──────────────────────────────────────────────────────────┤
  │ FLY_MID   │ Splines to (0, 0, -5.0) NED offset — the mid-chamber     │
  │           │ scan point                                               │
  ├───────────┼──────────────────────────────────────────────────────────┤
  │           │ Hovers here; checks max clearance across all 36 sectors. │
  │ FIND_PASS │  When any sector ≥ 5 m is found, that's the passage —    │
  │           │ flies into it                                            │
  ├───────────┼──────────────────────────────────────────────────────────┤
  │           │ Continuously refreshes waypoint from best scored sector  │
  │ LIDAR_NAV │ (clearance + goal bias) at the same altitude (MID_D =    │
  │           │ -5.0)                                                    │
  └───────────┴──────────────────────────────────────────────────────────┘

  Two helpers that make the waypoints clean:
  def _abs_ned(self, dn, de, dd):
      # home + offset  →  absolute NED
      return dict(x=home_n + dn, y=home_e + de, z=home_d + dd)

  If you need to tune the coordinates, they're at the top of the class:
  ENTRY_N, ENTRY_E, ENTRY_D = 0.0,  0.0, -2.2   # ← change here
  MID_N,   MID_E,   MID_D   = 0.0,  0.0, -5.0   # ← change here

✻ Churned for 4m 54s

※ recap: Building a LiDAR-guided cave drone explorer in ROS 2 with PX4 SITL.
   The script flies to the cave entry at (0,0,-2.2) then a scan point at
  (0,0,-5) where LiDAR detects the passage. Next: run the updated
  cave_explorer.py and test.


