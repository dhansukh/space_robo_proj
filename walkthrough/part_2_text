Plan approved — let's execute. I'll start by creating the task tracker and then work through each item.

Edited task.md

Let me first check the PX4 x500_base model to understand its structure, then read the existing gazebo_exporter to know what to fix:

Ran command: `cat /home/smalld/PX4-Autopilot/Tools/simulation/gz/models/x500_base/model.sdf`
Ran command: `cat /home/smalld/PX4-Autopilot/Tools/simulation/gz/models/x500/model.sdf`
Searched for "gazebo_exporter"

Now let me look at the gazebo_exporter to know what to fix, and check the existing sensor models in PX4:

Viewed gazebo_exporter.py:1-408

Now I have the full picture. Let me implement everything. I'll start with the most impactful files:

Edited cave_world.sdf

Now let me create the custom x500_cave model with sensors, the start script, bridge config, and launch file:

Created model.config
Created model.sdf

Now create the bridge config, start script, and launch file:

Created gz_bridge_px4.yaml
Created start_px4_cave.sh
Ran command: `chmod +x /home/smalld/Downloads/space_robo_proj/cave_drone_sim/scripts/start_px4_cave.sh`

Now create the PX4 airframe file and the ROS 2 launch file:

Created 4002_gz_x500_cave
Created px4_cave.launch.py

Now update the gazebo_exporter.py to include system plugins in future generated worlds, and update setup.py to install the new bridge config:

Edited gazebo_exporter.py

Now update setup.py to include the new bridge config:

Viewed setup.py:26-58

Good — the existing `config/*.yaml` glob already covers `gz_bridge_px4.yaml` since it uses `*.yaml`. And `launch/*.py` already covers `px4_cave.launch.py`. The setup.py is fine as-is.

Now let me update the task tracker and create the PX4 airframe symlink:

Edited task.md

Now let me set up the symlinks for PX4 to find the custom model and airframe:

Ran command: `ln -sfn /home/smalld/Downloads/space_robo_proj/cave_drone_sim/models/x500_cave /home/smalld/PX4-Autopilot/Tools/simulation/gz/models/x500_cave && echo "Model symlink created" && ls -la /home/smalld/PX4-Autopilot/Tools/simulation/gz/models/x500_cave`
Ran command: `cp /home/smalld/Downloads/space_robo_proj/cave_drone_sim/config/px4_airframes/4002_gz_x500_cave /home/smalld/PX4-Autopilot/ROMFS/px4fmu_common/init.d-posix/airframes/4002_gz_x500_cave && echo "Airframe copied" && ls -la /home/smalld/PX4-Autopilot/ROMFS/px4fmu_common/init.d-posix/airframes/4002_gz_x500_cave`

Now I need to register the airframe in PX4's CMakeLists. Let me check how existing airframes are registered:

Ran command: `cat /home/smalld/PX4-Autopilot/ROMFS/px4fmu_common/init.d-posix/airframes/CMakeLists.txt 2>/dev/null | grep -E "4001|x500" | head -5`

There's already a `4002_gz_x500_depth`! I need a different number. Let me check which IDs are available:

Ran command: `cat /home/smalld/PX4-Autopilot/ROMFS/px4fmu_common/init.d-posix/airframes/CMakeLists.txt | grep -E "^[[:space:]]+4[0-9]+" | sort`

ID 4002 is taken. Let me use 4050 for the cave exploration variant:

Ran command: `mv /home/smalld/PX4-Autopilot/ROMFS/px4fmu_common/init.d-posix/airframes/4002_gz_x500_cave /home/smalld/PX4-Autopilot/ROMFS/px4fmu_common/init.d-posix/airframes/4050_gz_x500_cave && echo "Renamed to 4050"`

Also update our local copy to use ID 4050:

Ran command: `mv /home/smalld/Downloads/space_robo_proj/cave_drone_sim/config/px4_airframes/4002_gz_x500_cave /home/smalld/Downloads/space_robo_proj/cave_drone_sim/config/px4_airframes/4050_gz_x500_cave && echo "Local copy renamed"`

Now register the airframe in PX4's CMakeLists.txt:

Ran command: `grep -n "4022_gz_x500_depth_mono" /home/smalld/PX4-Autopilot/ROMFS/px4fmu_common/init.d-posix/airframes/CMakeLists.txt`
Viewed CMakeLists.txt:1-122
Edited CMakeLists.txt

Now let me build the drone_bringup package to make sure everything compiles:

Ran command: `cd /home/smalld/Downloads/space_robo_proj/cave_drone_sim && source /opt/ros/humble/setup.bash && colcon build --packages-select drone_bringup --symlink-install 2>&1`

Need to run from the right directory structure. Let me check how the workspace is set up:

Ran command: `ls /home/smalld/Downloads/space_robo_proj/cave_drone_sim/ | head -20; echo "---"; ls /home/smalld/Downloads/space_robo_proj/cave_drone_sim/src/drone_bringup/resource/ 2>/dev/null`

The `cave_drone_sim` directory is the colcon workspace root (it has `build/`, `install/`, `src/`). The `colcon build` should be run from there:

Ran command: `cd /home/smalld/Downloads/space_robo_proj/cave_drone_sim && source /opt/ros/humble/setup.bash && colcon build --packages-select drone_bringup --symlink-install 2>&1 | tail -20`
Ran command: `cd /home/smalld/Downloads/space_robo_proj/cave_drone_sim && colcon list 2>&1 | head -20`

The issue is that `colcon` at the root only sees `cave_drone_sim` as a single meta-package (the root `package.xml`). The sub-packages in `src/` need to be discovered. Let me check the root package.xml:

Viewed package.xml:1-43

The root has its own `package.xml` which shadows `src/`. Colcon doesn't recurse into `src/` because it found a package at root level. Let me check if there's a `colcon.meta` that configures this:

Ran command: `cat /home/smalld/Downloads/space_robo_proj/cave_drone_sim/colcon.meta 2>/dev/null; echo "---"; colcon list --base-paths /home/smalld/Downloads/space_robo_proj/cave_drone_sim/src 2>&1`

The packages are in `src/`, but colcon at the root only sees the root package. We need to build with `--paths src/*`:

Ran command: `cd /home/smalld/Downloads/space_robo_proj/cave_drone_sim && source /opt/ros/humble/setup.bash && colcon build --packages-select drone_bringup --symlink-install --base-paths src 2>&1`

Build succeeded! Let me verify the installed files:

Ran command: `ls /home/smalld/Downloads/space_robo_proj/cave_drone_sim/install/drone_bringup/share/drone_bringup/launch/ && echo "---" && ls /home/smalld/Downloads/space_robo_proj/cave_drone_sim/install/drone_bringup/share/drone_bringup/config/`

All files are installed correctly. Let me now verify the launch file parses without errors:

Ran command: `cd /home/smalld/Downloads/space_robo_proj/cave_drone_sim && source install/setup.bash && python3 -c "
from launch import LaunchDescription
import importlib.util
spec = importlib.util.spec_from_file_location('px4_cave', 'src/drone_bringup/launch/px4_cave.launch.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)
ld = mod.generate_launch_description()
print(f'Launch description OK: {len(ld.entities)} entities')
for e in ld.entities:
    print(f'  - {type(e).__name__}: {getattr(e, \"name\", getattr(e, \"_name\", \"?\"))}')
" 2>&1`

Launch file parses cleanly with 7 entities (3 args + MicroXRCE + bridge + TF + RViz).

Let me finalize the task tracker and create the walkthrough:

Edited task.md
Edited walkthrough.md

All done. Here's a summary:

## Stage 2 Complete ✅

Created the full PX4 SITL + cave world integration pipeline. Here's what you need to run:

### Terminal 1 — PX4 + Gazebo:
```bash
cd ~/Downloads/space_robo_proj/cave_drone_sim
./scripts/start_px4_cave.sh
```

### Terminal 2 — ROS 2 stack:
```bash
cd ~/Downloads/space_robo_proj/cave_drone_sim
source install/setup.bash
ros2 launch drone_bringup px4_cave.launch.py
```

### What was created:
| File | Purpose |
|------|---------|
| `models/x500_cave/model.sdf` | PX4 x500 + LiDAR + depth cam + IMU |
| `scripts/start_px4_cave.sh` | Sets env vars, launches PX4 SITL |
| `config/gz_bridge_px4.yaml` | Sensor topic bridging |
| `launch/px4_cave.launch.py` | Micro XRCE-DDS + bridge + RViz |
| `4050_gz_x500_cave` | PX4 airframe (registered in CMakeLists) |
| `cave_world.sdf` | Fixed — added system plugins |
| `gazebo_exporter.py` | Fixed — future caves include plugins |

The drone spawns at `(6.19, 16.37, -6.5)` — the cave entry point, slightly above the marker to avoid ground collision. See the walkthrough for full details and verification commands.
